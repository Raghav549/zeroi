Alembic migrations for zeroi.

For production, generate migrations with:

    alembic revision --autogenerate -m "describe change"

The initial migration uses SQLAlchemy metadata creation for bootstrap convenience.
For long-lived production systems, replace with explicit autogenerated operations.
